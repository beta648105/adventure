// 게임 전역 설정값. 숫자 하나만 바꿔도 화면 전체가 따라옵니다.

// 논리 해상도(픽셀 단위). 실제 화면에는 정수배로 확대해서 그립니다.
export const VIEW_W = 256;
export const VIEW_H = 144;

// 타일 한 칸 크기. 지금은 안 쓰지만 맵을 붙일 때 기준이 됩니다.
export const TILE = 16;

// 기본 캐릭터 크기 (8 x 16)
export const CHAR_W = 8;
export const CHAR_H = 16;

// 이동 속도 (논리 픽셀 / 초)
export const WALK_SPEED = 48;

// 걷기 애니메이션 한 프레임당 유지 시간 (초)
export const WALK_FRAME_TIME = 0.14;

// 배경색 (흰색)
export const BG_COLOR = '#ffffff';
